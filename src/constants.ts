export const DEFAULT_PORT = 3666;
export const DEFAULT_WEBSOCKET_PORT = 3667;
export const LOCAL_HOST = `localhost:${DEFAULT_PORT}`;
export const LOCAL_WEBSOCKET_HOST = `localhost:${DEFAULT_WEBSOCKET_PORT}`;
